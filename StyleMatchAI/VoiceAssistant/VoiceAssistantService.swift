import Foundation

// Compatibility name retained for older call sites. All voice output now uses the single,
// corrected audio-session, state, interruption, route, reset, and logging implementation.
typealias VoiceAssistantService = VoiceStylistService
